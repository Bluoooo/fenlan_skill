# 粉蓝偶记 Skill · 使用说明

---

## 在 ChatGPT 上用（Custom GPT）

1. 打开 [chatgpt.com](https://chatgpt.com) → 左侧菜单 → "探索 GPT" → 右上角"创建"
2. 给 GPT 起名叫"粉蓝偶记"，写一句简介
3. 在 **Instructions** 框里，把 `SKILL.md` 的全文粘贴进去
4. 在 **Knowledge** 区域，上传 `articles.md`
5. 保存 → 选择"只有我"可以访问
6. 使用时，新开一个对话，@粉蓝偶记，然后像和我说话一样给它指令

**注意**：ChatGPT 的 Instructions 有字数限制（约 8000 字符），SKILL.md 约 5000 字，刚好够。如果超了，可以删掉末尾的"样本参照"段落。

---

## 在 Claude 上用（Projects）

1. 打开 [claude.ai](https://claude.ai) → 左侧"Projects" → 新建项目
2. 在 **Project Knowledge** 中：
   - 粘贴 `SKILL.md` 全文
   - 上传 `articles.md`（Claude Projects 支持大文件，210KB 没问题）
3. 在这个 Project 里新开对话，Claude 会自动加载全部风格知识
4. 给它指令即可

**优势**：Claude Projects 的文件支持最好，SKILL.md + articles.md 同时加载毫无压力。

---

## 在 DeepSeek / Kimi / 豆包等国产 AI 上用

国产 AI 大多不支持文件上传或 System Prompt，但你可以：

1. 新开对话
2. 先上传 `SKILL.md` 文件（如果支持），或直接粘贴全文
3. 再上传 `articles.md`，或粘贴其中几篇代表作
4. 告诉它"请以上述风格写一篇关于 XXX 的文章"

**技巧**：把 SKILL.md 保存成手机备忘录，每次复制粘贴方便。

---

## 在 WorkBuddy 上用（原生支持 / 自定义专家）

### 方式 A：直接调用已安装的 Skill

WorkBuddy 已经安装了「粉蓝偶记」Skill。在对话中说：

> 用粉蓝偶记的风格写一段关于 XXX 的文字

Skill 会自动加载，AI 将以你的风格写作。

### 方式 B：配置为自定义专家

如果你把 Skill 分享给其他人，或在新的 WorkBuddy 工作区使用：

1. 打开 WorkBuddy → 设置 → 专家（Experts）
2. 新建专家，命名为"粉蓝偶记"
3. 在 Prompt 区域粘贴 `SKILL.md` 的全文
4. 如果支持知识库文件，上传 `articles.md`
5. 保存后，在对话中切换为该专家即可

### 方式 C：作为角色定义（Role Prompt）

在 WorkBuddy 对话开头直接粘贴整段角色定义：

```
从现在开始，你是微信公众号「粉蓝偶记」的作者，请以以下风格写作：
[粘贴 SKILL.md 全文]

参考文章如下：
[粘贴 articles.md 全文，或上传文件]
```

然后给它写作指令。

---


